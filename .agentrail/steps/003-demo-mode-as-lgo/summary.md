Embedded --demo as a pre-built .lgo. Wrote cli/src/demo.s (the readable source with regen instructions in header), produced cli/src/demo.lgo via cor24-asm cli/src/demo.s -o cli/src/demo.lgo (76 bytes of L-line text). Replaced DEMO_SOURCE in cli/src/run.rs with DEMO_LGO via include_str!. Demo command branch now loads via EmulatorCore::load_lgo (no Assembler::new() in this path). cor24-emu --demo --speed 0 -n 1000 smoke-tested OK (LED counter still animates). cargo test --workspace 243 pass.