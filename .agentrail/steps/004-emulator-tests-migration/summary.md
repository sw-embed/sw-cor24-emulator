Migrated 6 #[cfg(test)] sites in src/emulator.rs that previously called Assembler::new(). Added asm_to_lgo() helper that spawns cor24-asm - reading source from stdin and returning the .lgo text on stdout. Each test now goes source -> cor24-asm -> .lgo -> EmulatorCore::load_lgo. After this commit src/emulator.rs has zero references to crate::assembler. cargo test --workspace 243 pass.