Persisted SPI master line state. IoState gains master_mosi (u8), master_sclk (bool), master_seln (bool) initialised to 0/false/true respectively (active-low SELN starts deselected). read_io / write_io for IO_SPI_DATA/SCLK/SELN now round-trip through these fields. Updated SPI MMIO unit tests to assert idle state and full round-trip behaviour. Added tmp125_drives_some_clocks integration test that samples SCLK and SELN in fine-grained 4-instruction batches early in the run and asserts SCLK is observed both edges and SELN goes active-low (slave selected) at least once — confirms spixchg.s makes bus progress. cargo test --workspace 263 pass (+1). clippy --all-features -- -D warnings clean.