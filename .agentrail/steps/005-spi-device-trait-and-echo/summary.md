Added src/peripherals/spi/ module mirroring i2c: SpiDevice trait, SpiHandle<D>, EchoDevice (latches MOSI for next exchange's MISO), build_spi_device registry. Wired SpiBusState with Option<Arc<Mutex<dyn SpiDevice>>>: on_select pre-loads shift_out, on_byte updates next-byte register, on_deselect on SELN rise. Added EmulatorCore::attach_spi_device(dev) -> SpiHandle<D>, attach_spi_device_shared, detach_spi_devices. Drive-by: tmp125_exchanges_bytes asserts last_miso_byte.is_some() rather than ==Some(0). All 284 tests pass; clippy clean.