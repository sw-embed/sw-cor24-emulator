Added UartLog type to IoState: records (direction, byte, instruction_count) for every UART TX write and RX send. UartLog::format() coalesces consecutive same-direction bytes into grouped lines (IN/OUT). Exposed via EmulatorCore::uart_log() and format_uart_log(). Cleared on reset. 13 new tests covering coalescing, echo patterns, escape chars, integration with CpuState I/O. All 224 tests pass. No existing output changed — consumers opt in by calling format_uart_log().