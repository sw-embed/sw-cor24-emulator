Deleted src/assembler.rs (1512 lines, the in-tree COR24 assembler library) and removed its pub use from src/lib.rs. Drive-by clippy fixes (state.rs collapsible_match; cli test_raw_binary_no_strip vec!->slice with OOB-aware coercion; cli test_load_raw_binary_file needless borrow) so the saga gate cargo clippy --workspace --all-targets -- -D warnings is clean. Tests: 203 pass; net -40 from baseline 243, all of which were assembler.rs-internal tests that disappeared with the module per the brief. Active code has zero references to cor24_emulator::assembler.